# Interoperability

This repository uses a public documentation layer model.

Public files should include title, responsible entity, source, version, date, path, licence, access level and validation status.

Future public releases should connect repository state, release tag, citation metadata, repository origin and archival identifiers.

Restricted material stays outside this public layer.
